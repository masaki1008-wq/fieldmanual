name: Firebase Deploy

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Deploy to Firebase (Hosting + Firestore Rules + Storage Rules)
        run: npx firebase-tools deploy --only hosting,firestore:rules,storage --token "${{ secrets.FIREBASE_TOKEN }}" --project fieldmanual-app --non-interactive
